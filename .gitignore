node_modules/
dist/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# pm2 (gerado no VPS, não vai no repo)
server/logs/

# Backup local que pode aparecer ao mexer no VPS via sftp
_archive*/

# Playwright (e2e local) — artefatos de execução. Os baselines em
# web/e2e/snapshots/ SÃO versionados de propósito.
test-results/
playwright-report/
playwright/.cache/
