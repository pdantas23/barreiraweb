# As variáveis de ambiente agora ficam no .env da RAIZ do monorepo.
# Veja /.env para o arquivo real.
# Este template serve de referência para deploy no VPS.

# === Web (Vite) ===
VITE_SERVER_URL=https://seu-dominio.com

# === Server ===
PORT=3001
DISCONNECT_TIMEOUT_MS=30000
NODE_ENV=production

# === Supabase (identidade persistente) ===
SUPABASE_URL=https://xwyzhcsvlylbrhpojltb.supabase.co
SUPABASE_SERVICE_KEY=cola-aqui-a-service-role-key
